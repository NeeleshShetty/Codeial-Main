<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/layout.css" />
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.7.0/jquery.min.js"></script>
    <!-- <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/noty/3.1.3/noty.min.css"
    />
    <script src="https://cdnjs.cloudflare.com/ajax/libs/noty/3.1.3/noty.min.js"></script> -->
    <%- style %>
  </head>

  <body>
    <%- include('_header') %> <% if(flash.success && flash.success.length > 0){
    %>

    <h5><%= flash.success %></h5>

    <% } %> <%- body %> <%- include('_footer'); %> <%- script %>

    <!-- <script>

      <% if(flash.success && flash.success.length > 0){ %>

              new Noty({
              theme:'realx',
              type:'success',
              text: '<%= flash.success %>',
              layout:'toopRight',
              timeout:1500

              }).show();

            <% } %>

            <% if(flash.error && flash.error.length > 0){ %>

              new Noty({
              theme:'realx',
              type:'error',
              text: '<%= flash.success %>',
              layout:'toopRight',
              timeout:1500

              }).show();

              <% } %>
    </script> -->
  </body>
</html>
